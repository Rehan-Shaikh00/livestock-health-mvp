# Legacy prototype (v1)

The original single-file hackathon prototype (server-rendered Jinja/Bootstrap).
Kept for reference only — the production app now lives in `server/` (API) and
`client/` (React SPA). Run from repo root with `python legacy/app.py` if needed
(it imports the shared `triage_engine`, `geo_engine`, `spatial`, `i18n` modules).
